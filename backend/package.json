{
  "name": "nur-stories-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Dependency-free moderation API and static server for the Nur PWA.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
